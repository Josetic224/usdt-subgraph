type Exchange @entity(immutable:true) {
  #Transaction Id
  id: ID!

  # Block Number
  block: BigInt!

  # sender of the token
  from: String!

  # Receiver of the token
  to: String!

  # Amount of the Token
  amount: BigInt!
}